[project]
name = "nn"
version = "0.1.0"
authors = [{ name = "Hinsun", email = "vanhoai.adv@gmail.com" }]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
nn = "nn:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
