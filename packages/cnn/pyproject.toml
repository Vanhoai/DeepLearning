[project]
name = "cnn"
version = "0.1.0"
authors = [{ name = "Hinsun", email = "vanhoai.adv@gmail.com" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cnn = "cnn:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
